# Massimo-specific homing extensions.
#
# Keep this file included after homing.cfg.
# Do not define G28 here until the final dispatch strategy is chosen:
# wrapping the existing homing_override may recurse because homing.cfg
# itself calls G28 internally.
